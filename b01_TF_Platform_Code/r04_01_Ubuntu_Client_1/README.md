# Ansible AWX

- Create ansadmin with sudo privileges

```sh
# Update the system
sudo apt update -y

# Install open ssh package
sudo apt install openssh-server -y # No require on AWS Cloud because already available
sudo systemctl status ssh
sudo systemctl start ssh





```
